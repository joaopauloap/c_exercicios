#include <stdio.h>
#include <stdlib.h>

int main(){
	int count;
	for(count=0;count<=100;count=count+1) printf("%d ", count);
	printf("\n \n");
	char letra;
 	for(letra = 'A' ; letra <= 'Z' ; letra =letra+1)
 	printf("%c ", letra); 
 	
	getchar();
	// for (count=1;count<=100;count=count+1) printf ("%d ",count); 
	
}
